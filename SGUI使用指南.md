# SGUI自动化测试：从登录到完成一次测试

平台入口：[打开 SGUI 工作台](http://127.0.0.1:8778/workbench)。登录页为 [SGUI 登录](http://127.0.0.1:8778/login)。已登录时访问登录页会返回工作台；要查看新登录页，可在右上角账号菜单中退出登录。

新版采用直接填写账号密码的登录表单，支持显示/隐藏密码、记住用户名、错误提示、中英文及明暗主题。使用原有账号密码，项目、用例和历史数据继续沿用。平台界面统一使用 SGUI 名称和图标，登录页、侧栏不再显示来源说明。

## 1. 每次开机怎样启动

在 Windows 文件资源管理器中打开 `D:\wharttest`，双击 **Start-Fusion.cmd**，等待显示服务启动完成，再访问上述网址。启动脚本会启动 Docker 服务和 Windows 自动化执行器。

也可以在 **PowerShell** 输入：

```powershell
& 'D:\wharttest\Start-Fusion.cmd'
```

用完后双击 **Stop-Fusion.cmd** 停止融合平台，以释放后台资源；**Status-Fusion.cmd** 查看运行状态。无需重新安装环境。源代码、运行数据和此次修改都位于 D 盘。

## 2. 登录并选择项目

输入原有用户名和密码，点击 **进入工作台**。如果忘记部署时保存的管理员凭据，在本机打开 `D:\wharttest\guicase-next\local\login-credentials.txt` 查看，文件含密码，请妥善保管。

顶部项目下拉框先选择 **融合验收示例**，这是已准备好演示数据的项目。要开始自己的业务测试，在左侧 **项目管理** 中创建项目，再从顶部切换过去。平台按项目隔离需求、知识库、用例和报告。

新注册普通账户需要管理员分配项目和相应权限。如果只看到少量菜单，先检查账户权限及项目成员设置。

## 3. 先体验现有内容，不必立即调用模型

进入 **图文用例工作台 → 生成记录与导入**，打开已有批次：

- **文字需求 → AI 用例 → 正式入库**：之前真实生成并保存的 3 条登录用例，点击 **查看正式用例**，可以查看名称、步骤、预期结果和待审核状态。
- **GuiCase 散客预订与出票 · 43 条预置用例**：示例草稿，可打开用例修改内容，点击 **校验并保存草稿**，再导出 Excel、Word、JSON 或 Feature。需要正式保存时点击 **确认存入用例管理**。

打开历史记录、编辑草稿、导入、导出和手动管理用例均不需要调用模型。JSON 保留完整字段，适合再次导入；Excel、Word 更适合评审与交付。

## 4. 自己生成一批文字用例

首次建议仅使用文字，把基础流程走通：

1. 在 **图文用例工作台** 点击 **新建任务**，进入 **需求与图片**。
2. 点击 **选择需求文档**上传 TXT、MD、PDF、DOCX，可一次多选、分批追加或拖放。**任务名称与需求说明均可留空**；AI 会根据材料归纳任务名称，手动填写的名称会保留。也可以只输入需求说明，或点击 **填入文字示例**。
3. **文字生成模型**选择当前可用的文字模型；当前部署已配置过 GLM-5.1。没有可用模型时，先到 **系统管理 → 模型配置**配置接口地址、模型名和 API Key。
4. 首次不上传截图、不勾选 DOCX 内嵌图片、不选择补充知识库。在 **模型、规则与提示词** 中可保留默认规则，AI 评审和自动化建议先不启用。
5. 点击 **生成用例草稿**，在 **用例结果**查看后台处理进度。刷新或离开页面后可从生成记录继续查看。
6. 点击用例名称，检查并修改前置条件、步骤、预期结果、级别及模块。处理规则错误后，点击 **校验并保存草稿**。
7. 点击 **确认存入用例管理**。正式用例初始为待审核，去 **用例管理**进行人工评审，也可以导出给他人检查。

模型生成会使用你已配置的 API 额度。规则校验通过表示格式满足要求，业务准确性仍需人工确认。

文档最多 20 份（含引用已有文档），单份 20 MB；待上传文档和图片合计最多 80 MB。图片最多 50 张（含选中的 DOCX 内嵌图片），单张 8 MB。直接输入的说明最多 10 万字符，合并需求材料最多 30 万字符。

图片每批最多 6 张，后台依次解析并保存进度；失败后点击 **从断点继续**，只继续未完成的批次。页面显示已完成的图片数和批次数。全部分析结果一起参与 AI 业务模块规划，传输批次不作为业务模块；图片越多，生成时间和模型调用量越大。缩略图区支持独立滚动与逐张移除。用例的截图引用来自其引用的图片分析材料，批次引用不代表该批每张图均被逐项覆盖。

长文档先按标题、表格和完整用例记录组织材料。可识别的 PDF 用例表格会合并跨页内容，保留操作和预期结果、原始用例编号及页码，再由 AI 根据业务内容规划模块与任务名称。没有清晰模块目录的正文先由 AI 逐单元通读，整理全部业务主题及前后依赖，再合并规划；生成时继续使用对应原文，业务主题摘要仅用于规划。进入 **用例结果 → AI 模块规划与生成进度**，可以查看模块范围、每个模块已完成的批次和用例数量。少数超长段落或单格仍需细分，会保留相邻重叠文字和同一来源；表格识别失败或正文经过编辑时优先使用当前保存的正文，并显示复核提示。扫描 PDF 仍需要先完成 OCR。

各批生成代表性用例，结果保留来源引用；资料全部归类不等于测试覆盖完整。完成后显示业务材料单元数和被用例显式引用的数量，请结合原文检查遗漏和跨模块流程。原始材料保留在需求管理，JSON 导出包含 `source_unit_ids` 和 `source_references`。模型容量不足或返回格式不符会显示错误，不会将不完整任务直接存入正式用例库。

点击生成前，可在文件旁点击 **移除**或清空列表；选过的同一文件可以移除后重新选择。提交后，文档会转为 **引用已有需求文档**，可点击标签 × 从下一次生成中移除引用，原文件仍保存在需求管理。

模型超时或连接异常时会有限重试；仍失败后，打开该记录点击 **从断点继续**，复用已完成的文档快照、模块方案及批次结果。续跑使用本次冻结的材料；更换材料需要点击 **调整材料后新建任务**，恢复已解析文档后增删或替换。新建任务若需要此前的截图，应重新选择图片或开启内嵌图片分析。修改模型连接配置后可以继续原任务；需要更换工作台阶段选择或提示词时应新建任务。

## 5. 实际运行一次 UI 自动化

在顶部保持选择 **融合验收示例**：

1. 左侧打开 **UI 自动化**，切换到 **执行器**，确认当前 Windows 执行器在线。若离线，重新运行 `Start-Fusion.cmd`。
2. 切换到 **测试用例**，找到 **SGUI 登录页 · 四步浏览器验证**。
3. 点击该用例的执行按钮，选择在线执行器；浏览器使用 Chromium，可选择无头模式执行。
4. 该演示会打开新版登录页、检查用户名输入框、点击输入框、检查密码输入框，验证页面及执行链路；不会提交账号密码。
5. 切换到 **执行记录**查看状态、步骤结果和对应记录中的截图/Trace（以实际生成的附件为准）。

工作台生成的是功能用例草稿。要自动验证自己的业务页面，需要补齐被测地址、元素定位器、测试数据和页面步骤，再组合为 UI 测试用例。上述演示只验证本平台登录表单，不代表历史生成的 3 条业务用例已经全部自动执行。

自己的 UI 测试通常按 **页面管理 → 配置元素 → 页面步骤 → 测试用例 → 执行 → 执行记录** 的顺序配置。接口测试则进入 **接口自动化**，配置接口、环境变量、断言，再组合用例和测试任务。手动执行稳定后，再使用 **任务中心**配置定时运行。

## 6. 每个模块的用途

| 入口 | 主要用途 |
| --- | --- |
| 图文用例工作台 | 文字/文档/图片输入，AI 草稿、规则校验、编辑、入库、导入导出 |
| 项目管理 | 项目、成员及资源归属 |
| 需求管理 | 需求文档保存、文字解析、需求评审 |
| 用例管理 | 正式功能用例、模块、测试步骤、人工审核 |
| 知识库 | 建立可检索资料，生成时提供补充上下文；检索需要相应 Embedding 配置 |
| UI 自动化 | 网页元素、操作步骤、浏览器执行器、执行记录 |
| 接口自动化 | HTTP 接口、环境、数据、断言、接口用例及报告 |
| 测试套件 / 执行历史 | 按套件组织原有测试执行流程并查看结果 |
| 智能助手 | 使用模型和配置好的工具进行测试辅助 |
| 任务中心 | 对已配置且能够正常执行的任务设置调度 |
| 系统管理 | 用户和权限、模型、MCP 工具及其他管理配置 |

## 7. 以后怎样开启图片生成

先在模型配置中接入真正支持图片输入的模型，并开启其图片能力；再在工作台选择 **图片解析模型**，上传截图或勾选 DOCX 内嵌图片。流程为图片解析、结合文字规划业务模块并生成、规则校验、人工确认、正式入库。能否使用图片以当前模型的实际接口能力为准；不上传图片时按纯文字流程运行。

图片解析、业务用例生成和浏览器执行是不同环节：上传图片本身不会触发对网站的操作。执行前仍需配置目标站点与可执行步骤。
