# SGUI自动化测试（GuiCase 风格与 WHartTest 融合版）

此版本以 WHartTest 2.7.2 的本地源码为基础，保留其许可和原有功能，采用 GuiCase 风格的图文用例工作台。2026-09-14 按用户要求统一平台名称为 SGUI自动化测试，并重新设计登录页；界面名称、图标、执行器名称和导出标识统一为 SGUI，来源信息保留在源码说明与许可文件中。新版操作流程见 [SGUI使用指南](./SGUI使用指南.md)。代码目录为 `D:\wharttest\guicase-next`，分支 `integration/guicase-style`。原始 `D:\wharttest\WHartTest` 和 `D:\wharttest\guicase` 保留。

**访问与启动**

- 访问：http://127.0.0.1:8778/workbench
- 在文件资源管理器中双击 `D:\wharttest\Start-Fusion.cmd` 启动；`Stop-Fusion.cmd` 停止；`Status-Fusion.cmd` 查看状态。
- 也可以在 Windows PowerShell 输入：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File D:\wharttest\guicase-next\local\Manage-Workbench.ps1 -Action Start
```

登录使用原 WHartTest 的账号密码。部署时复制了账号和项目数据库，随后两套数据独立演进，并非实时同步。管理员凭据另保存在 `D:\wharttest\guicase-next\local\login-credentials.txt`；不要提交该文件和 `.env`。

**功能归属与保留情况**

| 功能 | 融合方式与入口 |
| --- | --- |
| 账号、项目、成员和权限 | 直接沿用 WHartTest；新工作台同样校验用例权限和项目成员身份 |
| 需求上传、解析、评审 | 沿用 WHartTest；工作台上传的文档进入当前项目的需求管理 |
| 用例、步骤、模块、审核状态 | 正式数据写入 WHartTest 原有表；新用例为待审核状态 |
| 知识库、Embedding 与检索 | 沿用 WHartTest；工作台可选当前项目知识库作为补充依据 |
| 智能助手、MCP、Skills | 保留原入口和实现，模型配置继续由 WHartTest 管理 |
| API 自动化、UI 自动化、测试套件、任务中心、执行报告 | 保留 WHartTest 的原有功能和执行协议，原生 Windows 执行器已接到融合版 |
| GuiCase 视觉风格 | 深蓝侧栏、浅色背景、紧凑卡片与表格、左侧流程导航和右侧标签工作区；使用 SGUI 标识，移除登录页与侧栏的来源说明 |
| GuiCase 图文生成 | 已接入新工作台：纯文字、文档、截图可组合输入；图片解析 → 文字生成；文档内嵌图片可选 |
| GuiCase 分阶段模型 | 视觉、文字、AI 评审、自动化建议分别引用 WHartTest 模型配置；Embedding 归入 WHartTest 知识库配置 |
| GuiCase 五类提示词 | 保留图片解析、逐页分析、用例生成、AI 评审和自动化建议五类可编辑提示词，按项目保存 |
| GuiCase 规则引擎 | 迁入 14 条默认规则，支持必填、枚举、正则、禁止正则、长度、数量六类规则；错误阻止入库，建议允许入库 |
| GuiCase 用例分类与额外字段 | 保留源编号、测试场景、测试数据类型、目的、目的细分、摘要、标签、截图引用，以及步骤动作、对象、值、定位器等 |
| GuiCase 多格式导出 | 已接入 Excel、Word、完整 JSON、Gherkin Feature；已保存批次导出时读取最新正式用例 |
| GuiCase 预置用例 | 43 条散客预订与出票用例可载入预览，编辑后按需入库；原始数据仍保留 |
| 后台任务与生成记录 | 新增持久化任务、状态查询、取消后续处理、草稿版本检查和防重复保存；刷新或离开页面后后台继续处理 |

**仍保留在原版 GuiCase 的兼容工具**

旧资料库的文件夹扫描导入、SQLite 资料库备份/恢复、孤立文件检查、旧 browser-use CLI 和旧离线模板生成仍在 `D:\wharttest\guicase`，此次没有删除或改写。需要时双击 `D:\wharttest\Start-GuiCase.cmd`，访问 http://127.0.0.1:8777 。这些工具尚未重写成 WHartTest 的项目 API；它们操作原版资料目录，与融合版数据库分开。

检查时原版 GuiCase 活动知识库为 0 条，因此没有遗漏需要搬迁的活动资料。旧配置、ZIP 备份、历史文件均保持原位。以后从旧版导出的用例 JSON，可在融合版“生成记录与导入”中导入。原 browser-use CLI 作为历史兼容代码保留，融合版的主要执行路径采用 WHartTest；此次未安装或验证旧版 CLI 的可选依赖。

**数据转换约定**

| GuiCase | WHartTest |
| --- | --- |
| `tc_name` | `TestCase.name` |
| `precondition` | `TestCase.precondition` |
| 步骤 `seq / desc / expected` | `step_number / description / expected_result` |
| 高 / 中 / 低 | P0 / P1 / P2；若 JSON 明确提供 WHartTest 的 `level`，以该字段为准，P3 也保留 |
| 模块路径 | 创建对应项目的模块树；超过五层时将第五层及后续层级以 ` · ` 合并，完整原路径保存在 `original_module_path` |
| 场景、数据类型 | 在没有明确 `test_type` 时，异常场景映射 exception、边界值映射 boundary，其余 functional；原分类同时保留 |
| 其他属性、未知扩展属性 | 存入与正式用例一对一的扩展记录；完整 JSON 导出保留这些字段 |

按“当前项目 + 源编号”识别重复来源：重复导入显示重复数，确认时跳过已有条目，不覆盖正式编辑结果。缺少源编号时生成稳定编号。整批保存放在数据库事务中；规则错误或版本冲突不会写入部分正式用例。

旧 JSON 的 `source_images` 是截图名称引用，不能凭名称恢复不存在的图片。预置用例的引用文件未附在本次导入中，界面会提示。Excel、Word、Feature 是交付格式；完整字段往返导入请使用 JSON。

**立即体验的内容**

选择顶部项目“融合验收示例”：

1. 打开“图文用例工作台 → 生成记录与导入”。
2. 打开“文字需求 → AI 用例 → 正式入库”：这是使用现有 GLM-5.1 实际生成的 3 条登录用例，已通过规则检查并存入正式用例库，仍需人工审核业务准确性。
3. 点击“查看正式用例”，可以在原 WHartTest 用例管理中查看步骤和待审核状态。
4. 工作台还保留“GuiCase 散客预订与出票 · 43 条预置用例”草稿，可编辑、校验、导出或确认入库。
5. 打开“UI 自动化”，查看“SGUI 登录页 · 四步浏览器验证”。它由 WHartTest Windows 执行器执行，已经产生一条成功记录。四步为打开新版登录页、检查用户名输入框、点击输入框、检查密码输入框。最新验证记录见 `local/test-output/sgui-ui-check.json`。
6. 新建任务时可用“填入文字示例”，或上传自己的需求文档。模型调用会使用当前 WHartTest 配置并消耗相应 API 额度。

**多模态与执行边界**

当前 WHartTest 的 GLM-5.1 配置启用了文字调用，但 `supports_vision=False`。原 GuiCase 的视觉配置没有 API Key，因此尚未进行真实视觉模型调用。图片消息构造、能力检查和两阶段流程已通过模拟模型测试。接入真正支持图片的 OpenAI 兼容模型后，在模型配置中开启“支持图片输入”，再在工作台选择它，即可进行真实图片生成。

直接截图支持 JPG、PNG、WebP，单张 8 MB，最多 6 张；DOCX 内嵌图片与上传截图合计最多 6 张。TXT/MD/PDF/DOCX 文档单份最多 10 MB。扫描 PDF 的 OCR 尚未新增；不能提取文字时会提示，不会假装解析成功。

AI 生成的是功能用例草稿。“自动化”字段是分类标记，定位器和测试数据仍需有实际依据。自动化建议阶段只产生建议，不执行浏览器。实际 UI 执行继续使用 WHartTest 的页面、元素、页面步骤及 UI 用例；功能用例不会仅因标记为自动化就变成可运行脚本。此次成功的 UI 演示用例单独配置了定位器，验证的是登录入口流程，不能解释为 3 条生成用例全部自动执行通过。

模型报错或返回无效 JSON 时，任务明确失败，不会以离线示例冒充 AI 成功。取消操作会阻止后续阶段和结果写入，已经发出的模型请求可能仍计费。

**本地环境和回退**

代码、Node 依赖与缓存、媒体资料、执行器截图/trace 均放 D 盘。PostgreSQL、Redis、Qdrant 使用独立 Docker 卷，它们位于已迁到 D 盘的 Docker 虚拟磁盘内。没有新建 C 盘项目数据目录。

此版采用独立的七个服务，而不是共享原版数据库进程。为适配 16 GB 内存和现有 4 GB WSL 上限，启动融合版时会停止原 WHartTest 服务，数据库内容保留；两套 WHartTest 不建议同时运行。Celery 工作并发为 1，原生执行器并发为 1。GuiCase 原版是较轻的独立服务，按需启动。

数据库和向量库在原服务正常停止后复制，媒体文件单独复制。为避免副本自动触发旧定时任务，副本的已有调度被停用；需要时在新版本里重新开启。

回到原 WHartTest：先运行 `Stop-Fusion.cmd`，再运行原有 `Start-WHartTest.cmd`，访问 http://127.0.0.1:8913 。原版不自动接收融合版中新建的数据。

**验证依据与开发入口**

- 前端：Vue TypeScript 类型检查与生产构建通过。原仓库的缺失类型、接口页签方法、SVG 属性及重复翻译键一并修正；页签保存按项目区分。
- 后端：17 项测试通过；`guicase_workbench/tests.py` 覆盖权限、事务、版本冲突、重复入库、扩展字段往返、图片消息、模型错误不泄露密钥，以及导出文件解析。
- 真实调用：`local/test-output/acceptance.json`，记录 43 条预置用例导出、3 条真实模型生成、正式入库和重复保存检查。
- 真实执行：`local/test-output/actuator.json`，记录四步浏览器执行与持久化结果。
- 重启检查：`local/test-output/restart.json`，停止后重新启动，登录、用例草稿、正式用例和执行结果均保留。另已在浏览器表单中验证预置草稿的备注编辑、保存及规则重检。
- 本地构建日志：`D:\wharttest\logs\fusion-frontend-build.log`。原前端主包偏大，构建有体积提示；尚未对全部历史页面做完整回归测试。
- 代码入口：`Vue/src/views/GuiCaseWorkbenchView.vue`、`Django/guicase_workbench/`、`local/compose.yml`。
- WHartTest 原始 LICENSE、版权声明与源码来源记录保留；产品界面采用 SGUI 品牌。GuiCase schema、规则及导出器迁入 `guicase_workbench/vendor`，来源为本机用户提供的 GuiCase 代码。
